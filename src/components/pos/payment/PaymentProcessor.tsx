import React from 'react';

// Simple stub for PaymentProcessor - maintains interface compatibility
export const PaymentProcessor: React.FC<any> = (props) => {
  return null; // This component is no longer used in the clean system
};

export default PaymentProcessor;